{
  "name": "guidesearch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DCInside guide backup and static search site for GitHub Pages",
  "scripts": {
    "crawl": "node scripts/crawl.mjs",
    "crawl:test": "node scripts/crawl.mjs --test",
    "build": "npm run crawl",
    "check": "node --check scripts/crawl.mjs && node --check public/app.js && node --check public/restore-payload.js && node --check tools/guidesearch-restore.user.js && node --test scripts/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
